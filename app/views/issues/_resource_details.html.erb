        <%= @item.resource.medium.medium %><br />
        <%= pluralize(@item.resource.length, @item.resource.length_unit.downcase) %><br />
        <%= @item.non_event_availability %><br /><br />
        
        
