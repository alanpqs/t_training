<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <%= csrf_meta_tag %>
    <%= render 'layouts/stylesheets' %>
  </head>
  <body>
    <div class="container">
      <header>
        <%= render 'layouts/header' %>
      </header>
      <%= render 'layouts/left_panel'%>
      <section class="round">
        <div class="span-15">
          <%= yield %>
        </div>
        <%= render 'layouts/right_panel' %>
      </section>
    </div>
  </body>
</html>
