<%= form_for [:admin, @region] do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.label :region %><br />
    <%= f.text_field :region %>
  </div>
  <div class="actions">
     <%= submit_button(@tag_name) %>
  </div> 
<% end %>
